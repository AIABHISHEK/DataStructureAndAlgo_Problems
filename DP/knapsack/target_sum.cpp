#include<bits/stdc++.h>

#define ll long long int
#define int_ unsigned int

using namespace std;

//target sum
// we have to assign +/- in front each elements of the array so that on evaluating the value == given value
// we have to find the total number of the such combinations 
// problem is similar to the dividing the array in two subset and finding the difference (subset count with given difference)

// problem same as subset count with given difference

int main(){
ios_base::sync_with_stdio(false); // fastio
cin.tie(NULL); // fastio
int t;
cin >> t; 


return 0;
}